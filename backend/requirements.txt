# ============================================================================
# Backend dependencies.
# Modules 1-6 (Foundation, Auth, Session, ACL, Trust Score, Adaptive MFA).
# ============================================================================

# --- Web framework & server ---
fastapi==0.118.0
uvicorn[standard]==0.38.0

# --- Settings / config ---
pydantic==2.12.3
pydantic-settings==2.11.0

# --- Database ---
sqlalchemy==2.0.44
psycopg2-binary==2.9.11
alembic==1.17.1

# --- Redis ---
redis==6.4.0

# --- Authentication (Module 2) ---
PyJWT==2.10.1                # JWT encode/decode (pure-Python, no build step)
bcrypt==4.3.0               # password hashing, used directly (no passlib shim)
email-validator==2.2.0     # backs pydantic's EmailStr in schemas/auth.py

# --- Adaptive MFA (Module 6) ---

# --- WebSocket support (pulled in by uvicorn[standard] via websockets, listed
#     explicitly since Module 3 depends on it directly) ---
websockets==15.0.1

# --- CORS / utilities ---
python-multipart==0.0.20

# --- Testing ---
pytest==8.4.2
httpx==0.28.1
pytest-asyncio==1.2.0
fakeredis==2.31.3            # in-memory Redis for the session-lifecycle tests
