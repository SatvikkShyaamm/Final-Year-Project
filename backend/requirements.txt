# ============================================================================
# Backend dependencies — Module 1 (Foundation) baseline.
# Auth/MFA/Trust-score specific libraries (passlib, pyotp, python-jose, etc.)
# are added when their respective modules (2, 5, 6) are implemented, to avoid
# pulling in unused dependencies before they're needed.
# ============================================================================

# --- Web framework & server ---
fastapi==0.118.0
uvicorn[standard]==0.38.0

# --- Settings / config ---
pydantic==2.11.10
pydantic-settings==2.11.0

# --- Database ---
sqlalchemy==2.0.44
psycopg2-binary==2.9.11
alembic==1.17.1

# --- Redis ---
redis==6.4.0

# --- WebSocket support (pulled in by uvicorn[standard] via websockets, listed
#     explicitly since Module 3 depends on it directly) ---
websockets==15.0.1

# --- CORS / utilities ---
python-multipart==0.0.20

# --- Testing ---
pytest==8.4.2
httpx==0.28.1
pytest-asyncio==1.2.0
