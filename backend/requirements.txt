# ============================================================================
# Backend dependencies.
# Module 1 (Foundation) baseline + Module 2 (Authentication).
# MFA/Trust-score specific libraries (pyotp, etc.) are still added later with
# their owning modules (5, 6) to avoid pulling in unused dependencies early.
# ============================================================================

# --- Web framework & server ---
fastapi==0.118.0
uvicorn[standard]==0.38.0

# --- Settings / config ---
pydantic==2.11.10
pydantic-settings==2.11.0

# --- Database ---
sqlalchemy==2.0.44
psycopg2-binary==2.9.11
alembic==1.17.1

# --- Redis ---
redis==6.4.0

# --- Authentication (Module 2) ---
PyJWT==2.10.1                # JWT encode/decode (pure-Python, no build step)
bcrypt==4.3.0               # password hashing, used directly (no passlib shim)
email-validator==2.2.0     # backs pydantic's EmailStr in schemas/auth.py

# --- WebSocket support (pulled in by uvicorn[standard] via websockets, listed
#     explicitly since Module 3 depends on it directly) ---
websockets==15.0.1

# --- CORS / utilities ---
python-multipart==0.0.20

# --- Testing ---
pytest==8.4.2
httpx==0.28.1
pytest-asyncio==1.2.0
