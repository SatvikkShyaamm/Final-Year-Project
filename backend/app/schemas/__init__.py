"""
Pydantic request/response schemas package.

Empty in Module 1. Populated per-module alongside its endpoints, e.g.
schemas/auth.py (Module 2), schemas/session.py (Module 3), etc. Keeping
schemas separate from ORM models (app/models) keeps the API contract
independent of the DB schema.
"""
