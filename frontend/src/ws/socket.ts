/**
 * Placeholder for the WebSocket client (Module 3).
 *
 * Will open a persistent connection to the backend's session-signaling
 * endpoint right after login (mirroring the base paper's client behavior:
 * onopen -> session_open, onclose/onerror -> session_close), and will be
 * consumed by Module 8's live dashboard views for real-time updates.
 *
 * Left unimplemented in Module 1 because there is no session/auth to
 * signal yet — creating a socket here now would have nothing real to
 * connect to.
 */
export {}
