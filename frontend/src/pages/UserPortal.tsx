/**
 * Placeholder for the end-user portal (populated across Modules 2, 3, 5, 6):
 * session status, current trust score, access grant/restrict/revoke state,
 * and logout.
 */
export function UserPortal() {
  return (
    <div className="mx-auto max-w-2xl p-8 text-[color:var(--color-text)]">
      <h1 className="text-2xl font-semibold">User Portal</h1>
      <p className="mt-2 text-sm text-[color:var(--color-text-muted)]">
        Session status, trust score, and access state will appear here once Modules
        2, 3, 5, and 6 are implemented.
      </p>
    </div>
  )
}
