# Copy to frontend/.env (or .env.local) for native `npm run dev`.
# When running via Docker Compose, these are injected as build/runtime args
# from the root .env instead — see docker-compose.yml.

VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
